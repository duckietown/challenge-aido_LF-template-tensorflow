# challenge name(s); defaults to all compatible challenges
# challenge: [c1,c2]

protocol: aido2_db18_agent-z2
user-label: "template-tensorflow"
user-payload:
    features_required:
        gpu: 1
description: |
    This is a simple template for an agent that uses Tensorflow for inference.

    [This code is documented here](https://docs.duckietown.org/daffy/AIDO/out/tensorflow_template.html).
